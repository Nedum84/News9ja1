<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nigernewsheadlines">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:name=".VolleySingleton"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ActivityHome"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityViewPost"
            android:parentActivityName=".ActivityHome"
            android:theme="@style/AppTheme.SlidrActivityTheme">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActivityHome" />
        </activity>
        <activity
            android:name=".ActivityListNewsSources"
            android:parentActivityName=".ActivityHome"
            android:theme="@style/AppTheme.SlidrActivityTheme">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActivityHome" />
        </activity>
        <activity
            android:name=".ActivityListNewsCategory"
            android:parentActivityName=".ActivityHome"
            android:theme="@style/AppTheme.SlidrActivityTheme">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActivityHome" />
        </activity>
        <activity android:name=".ActivityBookmarks"
            android:parentActivityName=".ActivityHome"
            android:theme="@style/AppTheme.SlidrActivityTheme">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActivityHome" />
        </activity>

        <service
            android:name=".IntentServiceLoadDataFromServer"
            android:exported="false" />

        <receiver
            android:name=".ReceiverLoadDataFromServer"
            android:process=":remote" />
        <receiver
            android:name=".ReceiverWakefulBroadcast">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


    </application>

</manifest>